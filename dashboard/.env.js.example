/**
 * Dashboard Configuration Example
 * Copy this file to .env.js and fill in your site URLs.
 *
 * NOTE: WooCommerce API keys are no longer configured here.
 * They are injected server-side by the Nginx proxy.
 * See nginx/wc-auth.conf.example for setup instructions.
 */
window.JEWD_CONFIG = {
    wcBaseUrl: '/api/wc/v3',
    wpBaseUrl: '/api',
    statsEndpoint: '/jewd/v1/stats',
    siteUrl: 'https://jewelry.local.dev',
    adminUrl: 'https://jewelry.local.dev/wp-admin',
    perPage: 50,
    version: '2.0.0'
};
